This fixes a2159 screen brightness cap when using different model display.
it overrides brightness cap to force output. it works only in macOS. Windows and Linux has its own driver so there's no need for this app.
